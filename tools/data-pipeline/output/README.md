# Intermediate Output Directory

This directory is used as a temporary staging output directory for generated CSV files during pipeline extraction and transformation processes.

Once datasets are verified, cleaned, and validated, they are moved to the final dataset directory located at `CampusSeekers/data/final/<destination_category>/` for eventual Spring Boot ingestion.
